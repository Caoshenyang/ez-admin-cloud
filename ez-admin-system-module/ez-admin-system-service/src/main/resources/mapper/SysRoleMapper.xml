<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ez.admin.system.mapper.SysRoleMapper">

    <!--
        查询所有角色及其关联的权限标识列表
        关联角色菜单关系表和菜单表，获取每个角色对应的权限标识
    -->
    <resultMap id="RolePermissionVOMap" type="com.ez.admin.system.model.vo.RolePermissionVO">
        <id property="roleId" column="role_id"/>
        <result property="roleLabel" column="role_label"/>
        <collection property="permissions" ofType="string">
            <result column="menu_perm"/>
        </collection>
    </resultMap>

    <select id="selectAllRolePermissions" resultMap="RolePermissionVOMap">
        SELECT
            r.role_id,
            r.role_label,
            m.menu_perm
        FROM ez_admin_sys_role r
        LEFT JOIN ez_admin_sys_role_menu_relation rmr ON r.role_id = rmr.role_id
        LEFT JOIN ez_admin_sys_menu m ON rmr.menu_id = m.menu_id
        WHERE r.is_deleted = 0
          AND r.status = 1
          AND m.is_deleted = 0
          AND m.status = 1
          AND m.menu_perm IS NOT NULL
        ORDER BY r.role_id, m.menu_sort
    </select>

</mapper>
